name: Keep Render App Alive

on:
  schedule:
    - cron: "*/5 * * * *"   # Every 5 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render App
        run: |
          curl -s https://libya-pmm-dashboard.onrender.com > /dev/null || echo "Ping failed"
